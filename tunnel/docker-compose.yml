services:
  cloudflared:
    image: cloudflare/cloudflared:latest
    container_name: hermes-tunnel
    restart: unless-stopped
    env_file:
      - type: file
        path: /opt/data/credentials/tunnel.env          
        required: true
    command: tunnel run
